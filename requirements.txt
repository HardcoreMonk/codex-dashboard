# Codex Dashboard dependencies.
# Includes test tooling used by the documented verification commands.

fastapi==0.135.3              # web framework + Pydantic v2
uvicorn==0.44.0               # ASGI server (use --loop asyncio --http h11)
websockets==16.0              # /ws transport
h11==0.16.0                   # uvicorn http backend
aiofiles==25.1.0              # async file I/O
python-multipart==0.0.26      # form parsing (FastAPI dep)

prometheus_client>=0.20.0     # /metrics endpoint
watchdog>=4.0.0               # inotify-based file watching (polling fallback if missing)
pytest==9.0.3                 # test runner used by AGENTS.md verification
httpx==0.28.1                 # Starlette/FastAPI TestClient transport dependency
ruff==0.15.11                 # lint runner used by documented quality gates
bandit==1.9.4                 # security scanner used by CI and QUALITY-GATES.md
pip-audit==2.10.0             # dependency vulnerability scanner used by CI
